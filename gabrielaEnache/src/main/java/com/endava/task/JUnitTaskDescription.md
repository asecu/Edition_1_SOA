1. Write unit tests for Generics task.
2. Select 1-2 methods from each class (if there are methods for test) and write at least 2 unit tests for each method
3. Create separate Test Class per each Class from Generics (test classes should be located in src/test/java/generics/ package): Ex: Library.java -> LibraryTests.java
4. Use different matchers to assert the values (see list: http://hamcrest.org/JavaHamcrest/javadoc/1.3/org/hamcrest/Matchers.html )
5. Create a Suite Class that will contain all the test classes created above)